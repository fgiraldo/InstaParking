<%= simple_form_for(@additional) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :sypply %>
    <%= f.input :description %>
    <%= f.input :active %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
